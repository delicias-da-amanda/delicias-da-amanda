import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deliciasdaamanda.app',
  appName: 'Delicias da Amanda',
  webDir: 'dist'
};

export default config;
